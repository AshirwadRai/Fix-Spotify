<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- NoActionBar: the React UI draws its own chrome. The dark window
         background prevents a white flash between launch and first paint. -->
    <style name="Theme.FixSpotify" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">#121212</item>
        <item name="android:statusBarColor">#000000</item>
        <item name="android:navigationBarColor">#000000</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
